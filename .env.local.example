# --- Auth (required) ---
APP_USERNAME=System1
APP_PASSWORD=TestingAI
# Any random string >= 16 chars. Generate: openssl rand -base64 32
SESSION_SECRET=replace_me_with_a_long_random_string

# --- Google AI Studio (required) ---
GEMINI_API_KEY=your_google_ai_studio_api_key
# Override the default model if needed. Defaults to gemma-4-26b-a4b-it.
GEMINI_MODEL=gemma-4-26b-a4b-it
